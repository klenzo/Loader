$(function(){
	$.fn.Loaders = function (options) {
		console.log(options);

		var theme = options.theme, speed = options.speed, duration = options.duration;

	/*	console.log(theme);
		console.log(speed);
		console.log(duration); */
	}
});